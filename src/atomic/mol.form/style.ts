import { Sizes, Spacing } from 'app/styles';
import styled from 'styled-components';

export const FormStyled = styled.form`
  width: ${Sizes.Small};
  padding: ${Spacing.Small};
  margin: ${Spacing.Small};
`;
